const fs = require('fs');
const p = 'src/app/pages/Calendar.tsx';
let c = fs.readFileSync(p, 'utf8');

c = c.replace(
  'import { toast } from "sonner";\nimport {\n  calendarEvents,\n  calendarHours,\n  daysOfWeek,\n  weekLabel,\n  type CalendarEvent,\n} from "../data/mockData";',
  'import { toast } from "sonner";\nimport {\n  calendarEvents,\n  calendarHours,\n  daysOfWeek,\n  weekLabel,\n  type CalendarEvent,\n  type CalendarTaskItem,\n} from "../data/mockData";',
);

c = c.replace(
  '  ActionButton,\n  ConfirmDialog,\n  DeleteIconButton,\n  GlassPanel,\n  PageHeader,\n  PageTransition,\n  RoundedDatePicker,\n  RoundedTimePicker,\n  MemberChip,\n  cn,',
  '  ActionButton,\n  ConfirmDialog,\n  DeleteIconButton,\n  GlassPanel,\n  PageHeader,\n  PageTransition,\n  ProgressBar,\n  RoundedDatePicker,\n  RoundedTimePicker,\n  MemberChip,\n  cn,',
);

const taskTypeInsert = `function createTaskItem(label: string): CalendarTaskItem {\n  return {\n    id: \\`task-\\${Date.now()}-\\${Math.random().toString(36).slice(2, 8)}\\`,\n    label,\n    done: false,\n  };\n}\n\nfunction getTaskProgress(tasks: CalendarTaskItem[]) {\n  const total = tasks.length;\n  const completed = tasks.filter((task) => task.done).length;\n  const percent = total > 0 ? Math.round((completed / total) * 100) : 0;\n\n  return { completed, total, percent };\n}\n\n`;
if (!c.includes('function getEventResponsibleIds(event: CalendarEvent) {')) throw new Error('getEventResponsibleIds not found');
c = c.replace('function getEventResponsibleIds(event: CalendarEvent) {', taskTypeInsert + 'function getEventResponsibleIds(event: CalendarEvent) {');

c = c.replace('  addedById?: number;\n  completed?: boolean;\n  status: PostStatus;', '  addedById?: number;\n  completed?: boolean;\n  tasks?: CalendarTaskItem[];\n  status: PostStatus;');

c = c.replace('  const [pendingDelete, setPendingDelete] = useState<CalendarEvent | null>(null);\n  const [isCreateOpen, setIsCreateOpen] = useState(false);', '  const [pendingDelete, setPendingDelete] = useState<CalendarEvent | null>(null);\n  const [isCreateOpen, setIsCreateOpen] = useState(false);\n  const [taskDraft, setTaskDraft] = useState("");');

c = c.replace('    responsibleIds: [teamMembers[0]?.id ?? 1],\n    addedById: currentMember?.id ?? teamMembers[0]?.id ?? 1,', '    responsibleIds: [teamMembers[0]?.id ?? 1],\n    addedById: currentMember?.id ?? teamMembers[0]?.id ?? 1,\n    tasks: [],');

c = c.replace('      addedById: currentMember?.id ?? teamMembers[0]?.id ?? previous.addedById,', '      addedById: currentMember?.id ?? teamMembers[0]?.id ?? previous.addedById,\n      tasks: [],');

c = c.replace('      addedById: eventForm.addedById,\n    };', '      addedById: eventForm.addedById,\n      tasks: eventForm.tasks ?? selectedEvent.tasks ?? [],\n    };');

c = c.replace('      addedById: updatedEvent.addedById,\n    });', '      addedById: updatedEvent.addedById,\n      tasks: updatedEvent.tasks ?? [],\n    });');

c = c.replace('          addedById: createForm.addedById,\n          status: createForm.status,\n          date: createForm.date,\n          time: createForm.time,\n          completed: false,', '          addedById: createForm.addedById,\n          status: createForm.status,\n          date: createForm.date,\n          time: createForm.time,\n          completed: false,\n          tasks: [],');

c = c.replace('      addedById: selectedEvent.addedById ?? currentMember?.id ?? teamMembers[0]?.id,', '      addedById: selectedEvent.addedById ?? currentMember?.id ?? teamMembers[0]?.id,\n      tasks: selectedEvent.tasks ?? [],');

c = c.replace('    setEventForm((current) => (current ? { ...current, status: completedEvent.status } : current));', '    setEventForm((current) => (current ? { ...current, status: completedEvent.status } : current));');

const tasksBlockOld = `                    <div className="mt-5 rounded-[1.75rem] border border-border/60 bg-muted/20 p-4">\n                      <div className="flex flex-wrap items-center gap-3">\n                        <span\n                          className="inline-flex items-center rounded-full px-3 py-1 text-xs font-semibold"\n                          style={{ backgroundColor: \`${primaryMember.color}14\`, color: primaryMember.color }}\n                        >\n                          {selectedEvent.type}\n                        </span>\n                        <span className="text-sm font-medium text-foreground">{selectedEvent.description}</span>\n                      </div>\n                    </div>\n\n                    <div className="mt-6 rounded-3xl border border-border/60 bg-muted/20 p-4">`;
const tasksBlockNew = `                    <div className="mt-5 rounded-[1.75rem] border border-border/60 bg-muted/20 p-4">\n                      <div className="flex flex-wrap items-center gap-3">\n                        <span\n                          className="inline-flex items-center rounded-full px-3 py-1 text-xs font-semibold"\n                          style={{ backgroundColor: \`${primaryMember.color}14\`, color: primaryMember.color }}\n                        >\n                          {selectedEvent.type}\n                        </span>\n                        <span className="text-sm font-medium text-foreground">{selectedEvent.description}</span>\n                      </div>\n                    </div>\n\n                    <div className="mt-6 rounded-[1.75rem] border border-border/60 bg-white p-5 shadow-sm dark:bg-card/85">\n                      {(() => {\n                        const tasks = eventForm?.tasks ?? selectedEvent.tasks ?? [];\n                        const progress = getTaskProgress(tasks);\n\n                        return (\n                          <>\n                            <div className="flex flex-wrap items-center justify-between gap-3">\n                              <div className="flex items-center gap-3">\n                                <span className="inline-flex h-10 w-10 items-center justify-center rounded-2xl bg-primary/10 text-primary">\n                                  <CheckCircle2 className="h-5 w-5" />\n                                </span>\n                                <div>\n                                  <p className="text-xs uppercase tracking-[0.16em] text-muted-foreground">Atividades</p>\n                                  <p className="text-sm font-semibold text-foreground">{progress.completed} de {progress.total} atividades concluídas</p>\n                                </div>\n                              </div>\n                              <div className="flex flex-wrap gap-2">\n                                <ActionButton\n                                  variant="secondary"\n                                  onClick={() => {\n                                    const nextTask = createTaskItem(taskDraft.trim() || 'Nova tarefa');\n                                    setEventForm((current) => (current ? { ...current, tasks: [...(current.tasks ?? tasks), nextTask] } : current));\n                                    setTaskDraft('');\n                                  }}\n                                >\n                                  <Plus className="h-4 w-4" />\n                                  Adicionar item\n                                </ActionButton>\n                                <ActionButton\n                                  className="bg-primary text-primary-foreground shadow-lg shadow-primary/20"\n                                  onClick={() => {\n                                    setEventForm((current) =>\n                                      current\n                                        ? {\n                                            ...current,\n                                            tasks: (current.tasks ?? tasks).map((task) => ({ ...task, done: true })),\n                                          }\n                                        : current,\n                                    );\n                                  }}\n                                >\n                                  <CheckCircle2 className="h-4 w-4" />\n                                  Marcar todas como concluídas\n                                </ActionButton>\n                              </div>\n                            </div>\n\n                            <div className="mt-4 flex items-center justify-between text-sm text-muted-foreground">\n                              <span>{progress.completed} de {progress.total} atividades concluídas</span>\n                              <span>{progress.percent}%</span>\n                            </div>\n                            <ProgressBar value={progress.percent} max={100} />\n\n                            <div className="mt-4 space-y-3">\n                              {(tasks.length > 0 ? tasks : []).map((task) => (\n                                <div key={task.id} className="flex items-center justify-between gap-3 rounded-2xl border border-border/60 bg-background px-4 py-3">\n                                  <button\n                                    type="button"\n                                    onClick={() => {\n                                      setEventForm((current) =>\n                                        current\n                                          ? {\n                                              ...current,\n                                              tasks: (current.tasks ?? tasks).map((item) =>\n                                                item.id === task.id ? { ...item, done: !item.done } : item,\n                                              ),\n                                            }\n                                          : current,\n                                      );\n                                    }}\n                                    className="flex items-center gap-3 text-left"\n                                  >\n                                    <span\n                                      className={cn(\n                                        'inline-flex h-5 w-5 items-center justify-center rounded-md border',\n                                        task.done ? 'border-emerald-500 bg-emerald-500 text-white' : 'border-border bg-white text-transparent',\n                                      )}\n                                    >\n                                      ✓\n                                    </span>\n                                    <span className={cn('text-sm font-medium', task.done ? 'text-muted-foreground line-through' : 'text-foreground')}>\n                                      {task.label}\n                                    </span>\n                                  </button>\n                                  <button\n                                    type="button"\n                                    onClick={() => {\n                                      setEventForm((current) =>\n                                        current\n                                          ? {\n                                              ...current,\n                                              tasks: (current.tasks ?? tasks).filter((item) => item.id !== task.id),\n                                            }\n                                          : current,\n                                      );\n                                    }}\n                                    className="rounded-full border border-border/60 p-2 text-muted-foreground transition hover:bg-muted hover:text-foreground"
                                  >\n                                    ×\n                                  </button>\n                                </div>\n                              ))}\n                              {tasks.length === 0 ? (\n                                <div className="rounded-2xl border border-dashed border-border/60 bg-muted/20 p-5 text-sm text-muted-foreground">\n                                  Nenhuma atividade adicionada ainda.\n                                </div>\n                              ) : null}\n                              <div className="flex items-center gap-3 rounded-2xl border border-border/60 bg-muted/15 px-4 py-3">\n                                <input\n                                  value={taskDraft}\n                                  onChange={(event) => setTaskDraft(event.target.value)}\n                                  placeholder="Ex.: aprovar capa, revisar roteiro..."\n                                  className="min-w-0 flex-1 bg-transparent text-sm outline-none placeholder:text-muted-foreground/70"\n                                />\n                                <button\n                                  type="button"\n                                  onClick={() => {\n                                    const label = taskDraft.trim();\n                                    if (!label) return;\n                                    const nextTask = createTaskItem(label);\n                                    setEventForm((current) => (current ? { ...current, tasks: [...(current.tasks ?? tasks), nextTask] } : current));\n                                    setTaskDraft('');\n                                  }}\n                                  className="inline-flex h-10 w-10 items-center justify-center rounded-full bg-primary/10 text-primary transition hover:bg-primary/20"\n                                >\n                                  +\n                                </button>\n                              </div>\n                              <button\n                                type="button"
                                onClick={() => {
                                  const nextTask = createTaskItem(taskDraft.trim() || 'Nova tarefa');
                                  setEventForm((current) => (current ? { ...current, tasks: [...(current.tasks ?? tasks), nextTask] } : current));
                                  setTaskDraft('');
                                }}
                                className="mx-auto block text-sm font-semibold text-primary transition hover:opacity-80"
                              >
                                + Adicionar um item
                              </button>
                            </div>
                          </>
                        );
                      })()}
                    </div>

                    <div className="mt-6 rounded-3xl border border-border/60 bg-muted/20 p-4">`;
if (!c.includes(tasksBlockOld)) throw new Error('tasks block anchor not found');
c = c.replace(tasksBlockOld, tasksBlockNew);

c = c.replace(
  '                        {selectedEvent.completed ? "Concluída" : "Concluir atividade"}',
  '                        {selectedEvent.completed ? "Concluída" : "Concluir atividade"}',
);

fs.writeFileSync(p, c, 'utf8');
